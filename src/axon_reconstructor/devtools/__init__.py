"""Developer utilities.

This package contains small debug/validation helpers intended to be imported
from project-local scripts (e.g. under `projects/`) without requiring access to
`tools/` via `sys.path`.
"""
